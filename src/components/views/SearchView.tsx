import React from 'react'

import { SearchForm } from '../search/SearchForm'

export const SearchView: React.FC = () => {
  return (
    <div>
      <h1>Search</h1>
      <SearchForm />
    </div>
  )
}
